# Spam-Tools
SC spam SMS WA call :

$ apt update && apt upgrade

$ apt install python

$ apt install git

$ git clone https://github.com/i-djaini/Spam-Tools

$ cd Spam-Tools

$ pip install -r bahan.txt

$ python main.py
